
<!DOCTYPE html>
<html>
<head>
  <title>New Bot - Bot Manager</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.4.1/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    .modern-button {
      background: linear-gradient(135deg, #374151, #1f2937);
      border: 1px solid #d1d5db;
      transition: all 0.2s ease;
    }
    .modern-button:hover {
      background: linear-gradient(135deg, #1f2937, #111827);
      border-color: #9ca3af;
      transform: translateY(-1px);
    }
    .modern-input {
      border: 1px solid #d1d5db;
      transition: all 0.2s ease;
    }
    .modern-input:focus {
      border-color: #6b7280;
      box-shadow: 0 0 0 3px rgba(107, 114, 128, 0.1);
    }
    .file-input {
      border: 2px dashed #d1d5db;
      transition: all 0.2s ease;
    }
    .file-input:hover {
      border-color: #9ca3af;
      background-color: #f9fafb;
    }
  </style>
</head>
<body class="bg-gray-50 min-h-screen">
  <div class="min-h-screen p-6">
    <div class="max-w-2xl mx-auto">
      <!-- ヘッダー -->
      <div class="bg-white rounded-lg p-6 mb-6 border border-gray-200">
        <div class="flex items-center">
          <button onclick="window.location.href='/dashboard'" 
                  class="bg-gray-100 hover:bg-gray-200 text-gray-700 p-3 rounded-lg mr-4 transition-all duration-200 border border-gray-200">
            <i class="fas fa-arrow-left"></i>
          </button>
          <div>
            <h1 class="text-2xl font-semibold text-gray-900">
              <i class="fas fa-plus mr-3 text-gray-600"></i>新しいBot
            </h1>
            <p class="text-gray-500">新しいボットを追加</p>
          </div>
        </div>
      </div>

      <!-- フォーム -->
      <div class="bg-white rounded-lg p-6 border border-gray-200">
        <form method="POST" enctype="multipart/form-data">
          <div class="mb-6">
            <label for="name" class="block text-gray-700 font-medium mb-3">
              ボット名
            </label>
            <input type="text" 
                   name="name" 
                   id="name"
                   placeholder="ボット名を入力してください" 
                   class="modern-input w-full px-4 py-3 bg-gray-50 rounded-lg text-gray-900 placeholder-gray-400 focus:outline-none focus:bg-white" 
                   required />
          </div>

          <div class="mb-6">
            <label for="zip" class="block text-gray-700 font-medium mb-3">
              ZIPファイル
            </label>
            <div class="file-input rounded-lg p-6 text-center">
              <input type="file" 
                     name="zip" 
                     id="zip"
                     accept=".zip" 
                     class="hidden" 
                     required 
                     onchange="updateFileName(this)" />
              <label for="zip" class="cursor-pointer">
                <div class="text-gray-400 mb-2">
                  <i class="fas fa-cloud-upload-alt text-3xl"></i>
                </div>
                <p class="text-gray-600">ZIPファイルをアップロード</p>
                <p class="text-gray-400 text-sm mt-1">クリックしてファイルを選択</p>
              </label>
              <div id="fileName" class="mt-2 text-sm text-gray-600 hidden"></div>
            </div>
          </div>

          <div class="flex gap-3">
            <button type="submit" 
                    class="modern-button text-white px-8 py-3 rounded-lg font-medium flex-1">
              <i class="fas fa-upload mr-2"></i>アップロード
            </button>
            <button type="button" 
                    onclick="window.location.href='/dashboard'"
                    class="bg-gray-100 hover:bg-gray-200 text-gray-700 px-8 py-3 rounded-lg font-medium transition-all duration-200 border border-gray-200">
              キャンセル
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script>
    function updateFileName(input) {
      const fileName = document.getElementById('fileName');
      if (input.files && input.files[0]) {
        fileName.textContent = 'ファイル: ' + input.files[0].name;
        fileName.classList.remove('hidden');
      } else {
        fileName.classList.add('hidden');
      }
    }
  </script>
</body>
</html>
