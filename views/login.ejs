
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Login - Bot Manager</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.4.1/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    .modern-button {
      background: linear-gradient(135deg, #374151, #1f2937);
      border: 1px solid #d1d5db;
      transition: all 0.2s ease;
    }
    .modern-button:hover {
      background: linear-gradient(135deg, #1f2937, #111827);
      border-color: #9ca3af;
      transform: translateY(-1px);
    }
    .modern-input {
      border: 1px solid #d1d5db;
      transition: all 0.2s ease;
    }
    .modern-input:focus {
      border-color: #6b7280;
      box-shadow: 0 0 0 3px rgba(107, 114, 128, 0.1);
    }
  </style>
</head>
<body class="bg-white min-h-screen flex items-center justify-center">
  <div class="w-full max-w-md">
    <!-- ロゴセクション -->
    <div class="text-center mb-8">
      <div class="w-16 h-16 bg-gray-100 border border-gray-200 rounded-xl flex items-center justify-center mx-auto mb-4">
        <i class="fas fa-robot text-2xl text-gray-600"></i>
      </div>
      <h1 class="text-2xl font-semibold text-gray-900 mb-2">Bot Manager</h1>
      <p class="text-gray-500 text-sm">管理パネルにログイン</p>
    </div>

    <!-- ログインフォーム -->
    <form method="POST" class="bg-white border border-gray-200 rounded-xl p-6 shadow-sm">
      <div class="mb-6">
        <label class="block text-gray-700 text-sm font-medium mb-2">
          パスワード
        </label>
        <input type="password" 
               name="password" 
               placeholder="パスワードを入力してください" 
               class="modern-input w-full px-4 py-3 bg-gray-50 rounded-lg text-gray-900 placeholder-gray-400 focus:outline-none focus:bg-white" 
               required />
      </div>
      
      <button type="submit" 
              class="modern-button w-full text-white py-3 px-6 rounded-lg font-medium">
        ログイン
      </button>
    </form>

    <!-- フッター -->
    <div class="text-center mt-6">
      <p class="text-gray-400 text-xs">
        Bot Management System
      </p>
    </div>
  </div>
</body>
</html>
